> 🇬🇧 [English version](./EXERCISES.md) · [← მიმოხილვაზე დაბრუნება](./README_ka.md) · [📤 როგორ ჩააბაროთ](./SUBMITTING_ka.md)

# საშინაო დავალება 7 — სავარჯიშოები

შექმენით ქვემოთ ჩამოთვლილი ფაილები `submissions/<your-username>/`-ის შიგნით და დაწერეთ JavaScript კოდი თითოეული დავალების ამოსახსნელად.

გამოიყენეთ მხოლოდ ის, რასაც ბოლოში მოცემული [წესები](#წესები) უშვებს. თითოეული ფაილის გაშვება შეგიძლიათ ასე:

```bash
node exercise_1.js
```

ყოველი სავარჯიშო აჩვენებს თავის **მოსალოდნელ შედეგს**. თქვენმა პროგრამამ ზუსტად ის უნდა დაბეჭდოს. (მე-5 სავარჯიშოში ერთი ნაწილის შედეგი განზრახ დამალულია — იქ ეს წერია.)

როცა მნიშვნელობას წარწერასთან ერთად ბეჭდავთ, გადაეცით წარწერა და მნიშვნელობა **ორ არგუმენტად**: `console.log("add:", result);` გამოიყენეთ თითოეულ მოსალოდნელ შედეგში ნაჩვენები წარწერები.

---

## `exercise_1.js` — ერთი ფუნქცია, სამი გზა

დაწერეთ ფუნქცია, რომელიც აბრუნებს `3`-ზე გამრავლებულ რიცხვს — **სამჯერ**, ყოველ სტილში თითოჯერ:

1. `triple` — **ფუნქციის დეკლარაცია** (function declaration)
2. `tripleExpression` — `const`-ში შენახული **ფუნქცია-გამოსახულება** (function expression)
3. `tripleArrow` — **ისრიანი ფუნქცია** (arrow function) ფიგურული ფრჩხილებისა და `return`-ის გარეშე

შემდეგ დაწერეთ ეს სამი ხაზი:

```javascript
console.log(triple(4), tripleExpression(4), tripleArrow(4));
console.log(tripleArrow);
console.log(tripleArrow(4));
```

მოსალოდნელი შედეგი:

```text
12 12 12
[Function: tripleArrow]
12
```

დააკვირდით ბოლო ორ ხაზს. მრგვალი ფრჩხილების გარეშე `tripleArrow` არის **თავად ფუნქცია** — Node.js გაჩვენებთ, რომ ეს ფუნქციაა და რა ერქმევა. ფრჩხილებით `tripleArrow(4)` მას **გამოიძახებს** და შედეგს მიიღებთ.

---

## `exercise_2.js` — ფუნქციიდან ისრიან ფუნქციამდე

გადმოწერეთ ეს დეკლარაციები თქვენს ფაილში, შემდეგ **ყოველი მათგანი გადაწერეთ ისრიან ფუნქციად**, შენახულს `const`-ში იმავე სახელით. როცა დაასრულებთ, ძველი დეკლარაციები წაშალეთ.

```javascript
function square(n) {
    return n * n;
}

function isAdult(age) {
    return age >= 18;
}

function greet(name) {
    return `Hello, ${name}!`;
}

function area(width, height) {
    return width * height;
}

function sayHi() {
    return "Hi!";
}

function sign(n) {
    if (n > 0) {
        return "positive";
    } else if (n < 0) {
        return "negative";
    }
    return "zero";
}
```

ყოველი ისრიანი ფუნქცია დაწერეთ **რაც შეიძლება მოკლედ**:

* თუ სხეული ერთი გამოსახულებაა, ნუ იყენებთ **ფიგურულ ფრჩხილებსა და `return`-ს**
* თუ **ზუსტად ერთი** პარამეტრი არის, მის გარშემო მრგვალი ფრჩხილები შეიძლება მოაშოროთ: `n => n * n`
* **ნულ** ან **ორ** პარამეტრზე ფრჩხილები აუცილებელია: `() => …`, `(width, height) => …`
* `sign`-ის შიგნით `if` არის, ამიტომ მას ფიგურული ფრჩხილები და `return` **სჭირდება** — ეს ნორმალურია

შემდეგ დაამატეთ ეს ხაზი თქვენს ფაილში. მასში შეცდომაა:

```javascript
const half = n => { n / 2 };
```

შეასწორეთ `half` ისე, რომ რიცხვის ნახევარი დააბრუნოს, და მის ზემოთ **კომენტარში** ერთი წინადადებით ახსენით, რატომ აბრუნებდა თავდაპირველი ვერსია `undefined`-ს.

დაბეჭდეთ:

```text
square(7): 49
isAdult(16): false
greet("Nino"): Hello, Nino!
area(3, 5): 15
sayHi(): Hi!
sign(-4): negative
sign(0): zero
half(10): 5
```

> მინიშნება: `greet("Nino"):` წარწერა ორმაგ ბრჩხილებს შეიცავს. ეს წარწერა ერთმაგ ბრჩხილებში ჩასვით — `'greet("Nino"):'` — ან ორმაგი ბრჩხილების შიგნით `\"` გამოიყენეთ.

---

## `exercise_3.js` — ისრიანი ფუნქციები, რომლებიც ობიექტს აბრუნებენ

დაწერეთ სამი ისრიანი ფუნქცია. **არცერთი არ იყენებს ფიგურულ ფრჩხილებს სხეულად და სიტყვა `return`-ს** — თითოეული ობიექტს პირდაპირ აბრუნებს, ასე რომ მრგვალი ფრჩხილები არ დაგავიწყდეთ.

1. `createPerson(name, age)` — აბრუნებს ობიექტს `name` და `age` თვისებებით.
2. `createBook(title, pages)` — აბრუნებს ობიექტს `title`, `pages` და `isLong` თვისებებით. `isLong` არის `true`, როცა წიგნს `300`-ზე მეტი გვერდი აქვს, სხვა შემთხვევაში — `false`.
3. `wrap(value)` — აბრუნებს ობიექტს ერთი თვისებით, `value`. დაიწყეთ ამ მცდარი ვერსიიდან და შეასწორეთ ის:

   ```javascript
   const wrap = (value) => { value: value };
   ```

შემდეგ:

1. შეინახეთ `createPerson("Gigi", 25)` `const`-ში სახელად `person` და დაბეჭდეთ `person`.
2. დაბეჭდეთ `person.name`.
3. დაბეჭდეთ `createPerson("Nino", 30)`-ის `age` ისე, რომ ჯერ ცვლადში **არ** შეინახოთ.
4. დაბეჭდეთ `createBook("The Hobbit", 310)`.
5. დაბეჭდეთ `createBook("The Little Prince", 96)`.
6. დაბეჭდეთ `wrap(5)`.

მოსალოდნელი შედეგი:

```text
{ name: 'Gigi', age: 25 }
Gigi
30
{ title: 'The Hobbit', pages: 310, isLong: true }
{ title: 'The Little Prince', pages: 96, isLong: false }
{ value: 5 }
```

> მინიშნება: `pages > 300` უკვე `true`-ა ან `false`, ასე რომ შეგიძლიათ პირდაპირ `isLong`-ის მნიშვნელობად გამოიყენოთ.
>
> მე-3 ნაბიჯისთვის: როცა `createPerson("Nino", 30)` დაბრუნდება, ის *უკვე* ობიექტია, ასე რომ `.age` შეგიძლიათ პირდაპირ გამოძახების შემდეგ დაწეროთ.

---

## `exercise_4.js` — ფუნქცია მნიშვნელობაა

შექმენით ობიექტი სახელად `calculator` ოთხი თვისებით. **ყოველი თვისება ისრიანი ფუნქციაა**, რომელიც ორ რიცხვს იღებს:

* `add` — აბრუნებს `a + b`-ს
* `subtract` — აბრუნებს `a - b`-ს
* `multiply` — აბრუნებს `a * b`-ს
* `divide` — აბრუნებს `a / b`-ს

შემდეგ დაწერეთ პროგრამა სამ ნაწილად.

**ნაწილი 1 — გამოიძახეთ ფუნქციები ობიექტიდან.** დაბეჭდეთ თითოეულის შედეგი `8`-ით და `2`-ით.

**ნაწილი 2 — ფუნქციები მასივში.** შექმენით მასივი სახელად `operations`, რომელშიც `calculator`-ის ოთხივე ფუნქციაა (თანმიმდევრობით add, subtract, multiply, divide — **თავად ფუნქციები**, მათი შედეგები **არა**!). შემდეგ `for` ციკლით გამოიძახეთ მასივის ყოველი ფუნქცია `20`-ით და `5`-ით და დაბეჭდეთ თითოეული შედეგი.

**ნაწილი 3 — ფუნქცია ცვლადში.** შეინახეთ `calculator.add` `const`-ში სახელად `plus` (მრგვალი ფრჩხილების გარეშე). დაბეჭდეთ `plus(1, 2)`, შემდეგ დაბეჭდეთ თავად `calculator.add`.

მოსალოდნელი შედეგი:

```text
add: 10
subtract: 6
multiply: 16
divide: 4
25
15
100
4
3
[Function: add]
```

შემდეგ ფაილის ბოლოს, კომენტარში, უპასუხეთ ამ კითხვას:

**რა განსხვავებაა `const plus = calculator.add;`-სა და `const plus = calculator.add(1, 2);`-ს შორის?**

> მინიშნება: ციკლის შიგნით `operations[i]` ფუნქციაა — ასე რომ მას ისე გამოიძახებთ, როგორც ნებისმიერ სხვა ფუნქციას: არგუმენტებს მრგვალ ფრჩხილებში, პირდაპირ მის შემდეგ ჩასვამთ: `operations[i](20, 5)`.

---

## `exercise_5.js` — scope

ეს სავარჯიშო ორი ნაწილისგან შედგება.

### ნაწილი A — ჯერ ვარაუდ, მერე გაშვება

გადმოწერეთ ეს კოდი თქვენს ფაილში:

```javascript
let color = "red";

if (true) {
    let color = "blue";
    let size = 10;
    console.log("A:", color);
    console.log("B:", size);
}

console.log("C:", color);

function paint() {
    let color = "green";
    console.log("D:", color);
}

paint();
console.log("E:", color);
```

**ფაილის გაშვებამდე** კოდის ქვემოთ, კომენტარში, დაწერეთ, რა დაბეჭდავს, თქვენი ვარაუდით, ყოველი ხაზი:

```javascript
// My prediction:
// A: ...
// B: ...
// C: ...
// D: ...
// E: ...
```

ახლა გაუშვით ფაილი და შეადარეთ. თუ რომელიმე ვარაუდი მცდარი აღმოჩნდა, **ნუ შეცვლით მას** — მის გვერდით დაწერეთ კიდევ ერთი კომენტარი, რომელიც ახსნის, რა მოხდა სინამდვილში. A ნაწილის შედეგი აქ განზრახ არ არის ნაჩვენები: ფაილის გაშვება *თავადაა* შემოწმება.

შემდეგ კომენტარში უპასუხეთ ამ კითხვას: **რა მოხდებოდა, თუ A ნაწილის ყველაზე ბოლოს `console.log("F:", size);`-ს დაამატებდით, და რატომ?** სცადეთ, წაიკითხეთ შეცდომა, დაწერეთ პასუხი, შემდეგ ეს ხაზი ისევ წაშალეთ, რომ ფაილი გაეშვეს.

### ნაწილი B — შეასწორეთ შეცდომა

გადმოწერეთ ეს კოდი A ნაწილის ქვემოთ. ის ფასების შეკრებას უნდა აკეთებდეს, მაგრამ ჩავარდება:

```javascript
const prices = [4, 10, 6];

for (const price of prices) {
    let total = 0;
    total = total + price;
}

console.log("Total:", total);
```

შეასწორეთ ის **ერთი ხაზის გადაადგილებით**. ნუ დაამატებთ ახალ ხაზებს და ნუ გადაადგილებთ `console.log`-ს.

მთელი ფაილის შედეგის ბოლო ხაზი ასეთი უნდა იყოს:

```text
Total: 20
```

შემდეგ კომენტარში ახსენით: **რატომ ვერ ხედავდა `console.log` `total`-ს?** და ბონუს კითხვა: თუ ხედავდა კიდეც, რატომ **ისევ** არ მიიღებდით `20`-ს თავდაპირველი კოდით?

> მინიშნება: `let` ცხოვრობს მხოლოდ იმ `{ }` ბლოკის შიგნით, სადაც დაიწერა — ციკლის სხეული კი ბლოკია, რომელიც ყოველ გამეორებაზე თავიდან იწყება.

---

## `exercise_6.js` — სუფთა თუ არასუფთა?

ეს სავარჯიშო ორი ნაწილისგან შედგება.

### ნაწილი A — დაახარისხეთ ფუნქციები

გადმოწერეთ ეს კოდი თქვენს ფაილში:

```javascript
let score = 0;
const cart = ["Milk"];

const square = (n) => n * n;
const addPoints = (points) => {
    score = score + points;
    return score;
};
const fullName = (first, last) => `${first} ${last}`;
const rollDice = () => Math.floor(Math.random() * 6) + 1;
const addToCart = (item) => {
    cart.push(item);
    return cart.length;
};
const printSum = (a, b) => {
    console.log(a + b);
};
```

**ექვსივე ფუნქციისთვის** დაწერეთ კომენტარი: **სუფთაა** (pure) ის თუ **არასუფთა** (impure), და რამდენიმე სიტყვით ახსენით, რატომ. მაგალითად:

```javascript
// square — pure: it only uses its parameter and returns a value
```

კომენტარები შეგიძლიათ ქართულადაც დაწეროთ.

> მინიშნება: ყოველ ფუნქციას ორი კითხვა დაუსვით. **ცვლის ან ბეჭდავს რამეს საკუთარი თავის გარეთ?** **შეიძლება ოდესმე ერთსა და იმავე არგუმენტებზე სხვა შედეგი მოგცეს?** თუ რომელიმე კითხვაზე პასუხი „კი“ არის, ფუნქცია არასუფთაა.

### ნაწილი B — გახადეთ ისინი სუფთა

`addPoints`-ც და `addToCart`-ც ცვლიან ცვლადს, რომელიც ფუნქციის **გარეთ** ცხოვრობს. დაწერეთ თითოეულის სუფთა ვერსია:

1. `addPointsPure(currentScore, points)` — **აბრუნებს** ახალ ქულას. ის გარე `score` ცვლადს არც კითხულობს და არც ცვლის.
2. `withItem(list, item)` — **აბრუნებს ახალ მასივს**, რომელშიც `list`-ის ყველა ელემენტია და ბოლოს `item`. `list`-ი **არ** უნდა შეცვალოს.

შემდეგ:

1. დაბეჭდეთ `addPointsPure(0, 5)` **ორჯერ**.
2. დაბეჭდეთ `score`, რომ დაამტკიცოთ — ის არასდროს შეიცვალა.
3. შეინახეთ `withItem(cart, "Bread")` `const`-ში სახელად `bigger`.
4. დაბეჭდეთ `cart`, შემდეგ დაბეჭდეთ `bigger`.

მოსალოდნელი შედეგი:

```text
addPointsPure(0, 5): 5
addPointsPure(0, 5): 5
score: 0
cart: [ 'Milk' ]
bigger: [ 'Milk', 'Bread' ]
```

ერთი და იგივე შემავალი მნიშვნელობა, ერთი და იგივე შედეგი — ორჯერ ზედიზედ. `cart`-ში კი ისევ მხოლოდ `'Milk'` არის, რადგან `withItem`-მა ძველ მასივში `push`-ის ნაცვლად ახალი მასივი შექმნა.

> მინიშნება: მე-6 workshop-ის spread ოპერატორი მასივს აკოპირებს: `[...list]` ახალი მასივია იგივე ელემენტებით. კიდევ რა შეიძლება ჩაწეროთ ამ კვადრატულ ფრჩხილებში, `...list`-ის შემდეგ?
>
> A ნაწილის `addPoints`-ს და `addToCart`-ს თქვენს ფაილში არსად **ნუ** გამოიძახებთ — ეს `score`-ს და `cart`-ს შეცვლიდა და თქვენი შედეგი არ დაემთხვეოდა.

---

## წესები

შეგიძლიათ გამოიყენოთ:

* ოპერატორები (`+ - * / % ** += ++ === < >= && || !`), `.length`, მასივის ინდექსები, `Math`-ის მეთოდები
* `const` და `let`
* შაბლონური სტრიქონები და სტრიქონის მეთოდები
* `if` / `else if` / `else`
* `for`, `while` და `for...of` ციკლები
* მასივები, `.push()` და spread ოპერატორი `...` მასივის კოპირებისთვის
* ობიექტები და მათი თვისებები
* **ფუნქციის დეკლარაციები, ფუნქცია-გამოსახულებები და ისრიანი ფუნქციები**, პარამეტრები და `return`

**2–6 სავარჯიშოებში** ყოველი ფუნქცია, რომელსაც თავად ქმნით, დაწერეთ **ისრიან ფუნქციად**, თუ სავარჯიშო გადმოსაწერად დეკლარაციას არ გაძლევთ (მაგალითად, `paint` მე-5 სავარჯიშოში).

აკრძალულია **ამ დავალებაში**:

* `map`, `filter`, `find`, `some`, `every`, `reduce`, `forEach` და ნებისმიერი სხვა მეთოდი, რომელიც ფუნქციას იღებს არგუმენტად — ციკლი თავად დაწერეთ
* ნაგულისხმევი პარამეტრები (`const greet = (name = "stranger") => …`)
* `var` — გამოიყენეთ `const` ან `let`
* `prompt()` — ის Node.js-ში არ არსებობს, ამიტომ ფაილი ჩავარდება

ფუნქციის **სხვა ფუნქციისთვის გადაცემა** (callback) შემდეგი ნაბიჯია, და ზემოთ ჩამოთვლილი მასივის მეთოდები სწორედ მასზეა აშენებული. ეს დავალება ერთი ნაბიჯით ადრე ჩერდება: ჯერ ბოლომდე შეეჩვიეთ იმას, რომ ფუნქცია მნიშვნელობაა, რომელიც შეგიძლიათ შეინახოთ და გამოიძახოთ.

---

როცა თქვენი ექვსი ფაილი მზად იქნება, გადადით გვერდზე **[როგორ ჩააბაროთ](./SUBMITTING_ka.md)**.
